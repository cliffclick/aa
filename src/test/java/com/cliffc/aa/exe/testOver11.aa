// The type shows "M32:0=..." is pre-bound to the correct field
// Type: { - [[  27Call: A, 45Call: B, 33Load: 0=C:{ - D:- int64 -> int64 }, 37Call: D, 40Load: 1=E:{ - F:- flt64 -> flt64 }, 44Call: F]] G -> *[10]( _, int64, flt64) }
// Eval: <*[0]@{ _ },LAM12>
{ pred ->
  fx = (pair if pred ? { x -> (* x 2)} : { x -> (* x 3)}
             { x -> (f* x 0.5f) }
        );
  (pair (fx._ 2) (fx._ 1.2f))
}
