// Recursive data structure.  I can type this, but not evaluate it.
// Type: Cyclic reference to x
// Eval: CRASH
// To not crash, I need the pair to evaluate to a struct *first*, before
// evaluation arguments, and have that result ready for the 'x' lookup
x = ( pair 3 x );
x
