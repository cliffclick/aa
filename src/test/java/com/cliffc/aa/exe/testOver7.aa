// One DynLoad, fcn needs DynTable
// Type: int64
// Eval: 8

fcn = { ->
  @{
     qi ={ x -> (+1  x) }; // i -> i
     qf ={ x -> (f2i x) }; // f -> i
  }._    // Which of {q*} should be picked?
};
bar = { x -> ((fcn) x) };
if 1 ? (bar 7) : (bar 3.9f)
