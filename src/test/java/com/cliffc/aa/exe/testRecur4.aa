// Gen counters
// Simple test of captured environment
// Type: A:*[]( _, int64, { - -> A })
// Eval: *[]( _, 1, [LAMBDA,( $dyn=-,e3=1),B:( e3=[LAMBDA,B]),( $dyn=-),C:( e3=[LAMBDA,C]),()])
gen = { ->
  inc = { cnt ->
   (pair cnt { -> (inc (+ cnt 1))} )
  };
  (inc 0)
};
((gen).1)
