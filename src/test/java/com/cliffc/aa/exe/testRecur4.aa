// Gen counters
// Simple test of captured environment
// Type: A:*[2]( _, int64, { - - -> A })
// Eval: *[2]( _, 1, <*[6]@{ _ $dyn=-; cnt=1; },LAM5>)
gen = { ->
  inc = { cnt ->
   (pair cnt { -> (inc (+ cnt 1))} )
  };
  (inc 0)
};
((gen).1)
