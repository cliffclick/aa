// Has a real closure with side-effects
// GCP: *[25]( _, 0:=int64, 1:=int64, 2:=int64)
// HMT: *[25](_,int:int64,int:int64,int:int64)
gen = { init ->
  @{
    fld := init;
    get = { -> fld };
    inc = { -> fld := fld+1; };
  }
};
A = gen(3);
B = gen(7);
(A.get(),B.inc(),B.get())
