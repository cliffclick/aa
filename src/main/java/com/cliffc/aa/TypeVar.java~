package com.cliffc.aa;

import java.util.Arrays;
import java.util.HashMap;

/** an implementation of language AA
 */

// Supports type unification via Tarjan U-F
//

// c2 notes: dual not a flag, one of the base enum types.
// each type lazily caches its dual, so computing dual is a field load, then null check
// hashcons trick, recycle unused types.  ptr-eq not equals().
// base types only have the dual-field, easy to share
// different base types for float/double, byte/char/short/int/long


public class TypeVar {
